name: hg-workspace

services:
  backend:
    build:
      context: ./backend
      target: runtime
    environment:
      HG_ENV: ${HG_ENV:-development}
      HG_DATA_DIR: ${HG_DATA_DIR:-/data}
      HG_DATABASE: ${HG_DATABASE:-/data/hg.db}
      HG_BACKEND_PORT: ${HG_BACKEND_PORT:-8000}
      HG_TASK_WORKER_ENABLED: ${HG_TASK_WORKER_ENABLED:-true}
      HG_TASK_POLL_INTERVAL: ${HG_TASK_POLL_INTERVAL:-0.5}
    ports:
      - "${HG_BACKEND_PORT:-8000}:8000"
    volumes:
      - ./backend/app:/app/app:ro
      - hg_data:/data
    command:
      - uvicorn
      - app.main:app
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --reload
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3).read()"
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  backend-test:
    profiles: ["test"]
    build:
      context: ./backend
      target: test
    command: ["python", "-m", "pytest", "-q"]

volumes:
  hg_data:
